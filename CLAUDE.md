# Gloss

macOS 菜单栏翻译工具：复制文本 → 全局热键 → 浮层流式译文。个人日常工具，追求质感而非功能数量。

## 名字的由来

**gloss**，语言学术语「行间译注」：外语书页行间那行小字翻译（interlinear gloss），glossary 同源，词根是希腊语 γλῶσσα——「舌头、语言」。英语里同拼写的另一个词义是「光泽、润色」。翻译 + 润色，一词双关。

## 气质准则

写代码、做设计、加功能前，对齐这五条：

1. **如行间注**——工具是配角。注释贴着原文，不替代原文：无 Dock 图标、无主窗口，热键唤起、用完即走，绝不打断用户手头的事。
2. **简短**——gloss 的本义就是「简注」。每加一个功能先问：这还是一条注释，还是已经写成了一篇论文？功能宁缺毋滥。
3. **有光泽**——polish 是名字的另一半。流式输出的顺滑、浮层的动效、快捷键的手感，细节质感优先于功能数量。
4. **忠实**——注错了比不注更糟。翻译准确性、术语一致性高于速度和花哨。
5. **只 gloss，不 gloss over**——不掩盖、不含糊。原文永远可见可回；出错（网络、API）时如实呈现，不吞错误。

## 视觉标识

- 图形语言「行间注」：淡墨文本线之间，一条更粗短、发光的金色注线。形状唯一来源：`Design/AppIcon.svg`、`Design/MenuBarIcon.svg`
- 色板：墨底 `#17203A → #0B101F`；淡墨线 `#56648A`；金色注线 `#FFE29E → #F2A83B`
- 菜单栏图标是严格 template image：纯黑 + alpha，无渐变无发光（发光只属于 App icon）；两元素构图，避免汉堡菜单误读
- PNG / .icns / 各尺寸导出在 v1 开工时按 Apple 官方模板做，不手改中间产物

## 技术基调

- SwiftUI 菜单栏 App（MenuBarExtra，macOS 13+），零 Dock 存在感
- 翻译层只有一个 OpenAI 兼容 Provider：baseURL / apiKey / model 可配置，换服务商 = 换配置，不为任何一家写专属 client（单一数据源，不一致在结构上不可能）
- API key 只进 Keychain，不进 UserDefaults / 明文文件
- SSE 流式输出（URLSession.bytes），长文本边翻边显示
- v1：剪贴板 + 热键 + 浮层 + 设置页，零系统权限；v2 再谈划词、历史、润色
